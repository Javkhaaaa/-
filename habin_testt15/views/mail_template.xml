<odoo>
  <record id="email_template_invoice_send" model="mail.template">
    <field name="name">Invoice Email Template</field>
    <field name="model_id" ref="model_habin_order_detail"/>
    <field name="subject">Your Booking Invoice</field>
    <field name="email_to">${object.guest_id.email}</field>
    <field name="body_html">
      <![CDATA[
        <p>Hello ${object.guest_id.name},</p>
        <p>Attached is your invoice for your recent booking at ${object.listing_id.house_name}.</p>
        <p>Total Price: <strong>$${object.total_price}</strong></p>
        <p>Thank you for staying with us!</p>
      ]]>
    </field>
  </record>
</odoo>
